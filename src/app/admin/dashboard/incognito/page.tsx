// components
import IncognitoContent from "./content";

// metadata
import { Metadata } from "next";

export const metadata: Metadata = {
    title: `Edit Incognito Profile`
}

export default function Page() {
    return <IncognitoContent />
}